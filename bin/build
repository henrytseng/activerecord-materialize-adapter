#!/bin/bash

set -euo pipefail

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
cd $SCRIPT_DIR/..

mkdir -p .bundle
docker-compose build
docker-compose run \
  adapter \
  bundle install --path=.bundle
